(comment (include "../scripts/library.dl"))

(var a 0)
(var b 1)
(var c 0)
(var i 0)

(while (< i 10)
  (print a)
  (print "\n")

  (setq c (+ a b))
  (setq b a)
  (setq a c)
  
  (setq i (+ i 1)))

(print "Done")
(print "\n")
