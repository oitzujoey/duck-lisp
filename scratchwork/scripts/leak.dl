(print "Starting leak\n")
(var x ())
(while true

  (comment  Test stack)
  5
  ()
  
  (comment  Force conses to be popped)
  (setq x (cons (cons () ()) ()))

  (comment  Force objects to be popped)
  (setq x (cons (cons 1 2) 3)))
