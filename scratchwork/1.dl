(
 (string first-letter "T")
 (string rest "his is the song that never ends.\nYes, it goes on and on, my friends.\nSome people started singing it not knowing what it was,\nand they'll continue singing it forever just because\nt")
 (print-string first-letter)
 (label loop)
 (
  (comment "Might as well push a scope here for indentation.")
  (print-string rest)
  (goto loop)))
